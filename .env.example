# Google Gemini API key used by the AI chatbot widget (src/lib/gemini.js).
# Get a free key (no credit card required) at https://aistudio.google.com/apikey
#
# WARNING: this key ships in the client bundle and is visible to anyone who
# inspects network requests or devtools. In Google AI Studio / Google Cloud
# Console, restrict the key to your site's domain (HTTP referrer restriction)
# before going live. For zero exposure, proxy this call through a serverless
# function (e.g. a Vercel API route) that holds the key server-side instead.
VITE_GEMINI_API_KEY=AIzaSyxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
